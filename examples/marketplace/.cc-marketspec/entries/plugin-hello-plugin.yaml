# Presentation overlay for hello-plugin. Every field is optional — native data
# (plugin.json, SKILL.md) already yields a valid manifest; this only enriches it.
# yaml-language-server: $schema=../../../../schemas/entry.schema.json
group: examples
tagline: Say hello, the headless way
intro: A minimal plugin used to demonstrate how entry.yaml enriches native data.
skills:
  - name: greet
    trigger: When you want a friendly greeting in a demo.
    examples:
      - "say hi to me"
tips:
  - This whole plugin exists only to illustrate the manifest shape.
