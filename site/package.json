{
  "name": "cc-marketspec-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "npm run build --prefix .. && node ../dist/cli.js ..",
    "dev": "npm run generate && astro dev",
    "build": "npm run generate && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
